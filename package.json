{
  "scripts": {
    "build": "rollup -c rollup.config.mjs",
    "push": "npx grunt screeps",
    "deploy": "npm run build && npm run push",
    "watch": "rollup -c rollup.config.mjs --watch"
  },
  "dependencies": {
    "grunt-screeps": "^1.5.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-typescript": "^12.3.0",
    "@types/screeps": "^3.4.0",
    "dotenv": "^17.4.2",
    "grunt-cli": "^1.5.0",
    "rollup": "^4.60.3",
    "tslib": "^2.8.1",
    "typescript": "^6.0.3"
  }
}
